
```shell
git tag v0.1.0
git push origin v0.1.0
git push --tags
```

### Release Candidate

```shell
git tag v0.1.0-rc.1
git push origin v0.1.0-rc.1   
git push --tags
```

### Release packing

```shell
npm pack
```

